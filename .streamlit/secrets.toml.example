# Local dev only — copy to local .streamlit/secrets.toml. The deployed app (Render) uses
# these same values as plain env vars, set in Render's dashboard per render.yaml, not this file.
SUPABASE_URL = "https://YOUR_PROJECT.supabase.co"
SUPABASE_ANON_KEY = "your-anon-or-publishable-key"
