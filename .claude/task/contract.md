# Task contract
> DISPOSABLE. **Owns:** THIS task's objective, `scope_paths`, reserved decisions and
> `done_when`.
> **Never:** anything that outlives the task. Overwritten by the next one.

objective: Replace the three catalogue `applicability` sentences that say "banks" for rules
  that apply to the whole `financial` type. Issue #12.

scope_paths:
  - dbt_analytics/seeds/metric_catalogue.csv
  - frontend/metrics.json
  - tests/tooling/test_metric_catalogue.py
  - .claude/task/contract.md
  - .claude/task/review.md
  - .claude/active_work.md

decisions_reserved:
  - The wording, owner-set in chat (§6). Each replaces only the "banks" sentence; the rest of
    each string stays:
    `debt_to_equity`: "Shown only for operating companies: for a financial institution, the
    debt-to-equity ratio is central to its core business, so the ratio has a different meaning
    in that context."
    `current_ratio_stmt`: "Shown only for operating companies. Banks and insurance companies do
    not provide a breakdown into current and non-current liabilities, and the ratio does not
    have the same significance for the rest of the financial sector."
    `working_capital`: "Shown only for businesses with no turnover, where the question of
    short-term financial reserves is a key concern."
  - The two pedagogical bank mentions on `net_margin_pct` and `roa_pct` stay, per the issue.

done_when:
  - The three strings are replaced; the CSV is rewritten through the csv module, re-parsed,
    and every row still has 21 columns.
  - `frontend/metrics.json` regenerated by `scripts/export_metric_definitions_json.py` and
    `test_regenerated_json_matches_committed` passes.
  - A test keyed off `applies_to` asserts no row withheld from `financial` mentions banks,
    except the current-split sentence the owner wrote for `current_ratio_stmt`; rows shown
    for financials may still name banks (revenue pedagogy on `net_margin_pct`, `roa_pct`).
  - NOT changed, noted: `statement_roe_pct`'s applicability ends "Means something different
    for banks", the same shorthand for a metric that is shown for the whole financial type;
    not in the issue's list and not in the owner's approved wording, so it stays for the
    owner to word.

impact_map: Catalogue text and its JSON export; no screen renders `applicability` today, so
  nothing user-visible changes. No formula, label, format or eligibility change.
