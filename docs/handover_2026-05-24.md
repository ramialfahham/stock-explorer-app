# Session handover — Stock Swipe App (2026-05-24)

## Status update (2026-06-09)

- **Product docs:** refreshed in [`product_roadmap_2026-06.md`](product_roadmap_2026-06.md) and [`north_star.md`](north_star.md) through PR #125.
- **UX shipped:** Saved headlines, Discover card trim (no browse/live-quote/Quick check), clickable Saved rows, inline nav ⋯ menu.
- **Backlog:** Discover metric filters (#109), disclosure reuse in Learn panel (#112).
- **Ops:** export health gate (#116); weekly `data_pipeline.yml` + Streamlit Cloud deploy after export.

For current priorities and smoke checklist, use the roadmap — this handover file is historical context.

## Status update (2026-06-04)

- **Phase B on `main`:** fundamentals ingest, dbt pipeline (10 models), export, Streamlit (`frontend/app.py`), weekly `data_pipeline.yml`.
- **dbt quality:** Phases 1–4 merged (YAML tests, unit tests, singular tests); hybrid SQL §1.1 enforcement (SQLFluff + `check_dbt_sql_structure.py`).
- **CI:** `ci-validate` — layer contract, registry sync, SQL structure, `check_dbt_tests.py`, full `dbt build` on fixtures, documentation gate.
- **Agent workflow:** post-merge sync on `main` + delete merged branches (PR #19).
- **Open issue:** [#14](https://github.com/ramialfahham/stock-swipe-app/issues/14) Phase 5 test-strategy docs + `check_dbt_tests.py` (in flight).
- **Next (ops):** **Data Pipeline** `workflow_dispatch` re-triggered 2026-06-04 (secrets include `SUPABASE_DB_HOST`); confirm green + export row counts.
- **Next (product):** Streamlit Cloud deploy per [`streamlit_deploy.md`](streamlit_deploy.md); Saved headlines + UX polish (see `north_star.md`).

Historical blocker notes below (Supabase 403) are **resolved** — do not treat as current.

---

Read this first in a fresh chat. It captures project state, decisions, and the **resolved blocker** as of end of session 2026-05-23.

**Repo:** https://github.com/ramialfahham/stock-swipe-app  
**Supabase project ref:** `nkfxrrazavlawggylbgd`  
**Supabase URL:** `https://nkfxrrazavlawggylbgd.supabase.co`

---

## TL;DR — start here tomorrow

**Blocker:** GitHub Actions `supabase-migrate` workflow is **red**. Latest failure (run `26344741064`, 2026-05-23 22:04 UTC):

```
apply_supabase_migrations: HTTP Error 403: Forbidden
```

This happens when CI calls the Supabase Management API to auto-resolve the pooler host (`GET /v1/projects/{ref}/config/database/pooler`). The same **403 reproduces locally** with the token in `.env` — so this is **not a GitHub-only issue**; the access token cannot read that endpoint (wrong scope, wrong token type, or endpoint restricted).

**Fastest fix path (recommended):**

1. Run locally (works on user's machine; direct DB connection already succeeded for migrations):
   ```bash
   python scripts/discover_supabase_db_host.py
   ```
2. Set GitHub secrets from output:
   - `SUPABASE_DB_HOST` = hostname only (e.g. `aws-1-eu-central-2.pooler.supabase.com`)
   - `SUPABASE_DB_PORT` = `5432` (session pooler) or whatever the script reports
   - Keep existing: `SUPABASE_URL`, `SUPABASE_DB_PASSWORD`
3. Re-run **supabase-migrate** (Actions → workflow_dispatch).  
   With `SUPABASE_DB_HOST` set, the script **skips** the Management API and builds the pooler URI directly (see resolution order below).

**Do not paste secrets into chat or commit `.env`.**

---

## Current blocker — full history

| Attempt | Error | Cause |
|---------|-------|-------|
| Direct `db.*.supabase.co` in CI | `Network is unreachable` (IPv6) | GitHub runners can't reach direct DB host |
| Hand-built `SUPABASE_DB_URL` with example host `aws-0-eu-central-1` | `Tenant or user not found` | Wrong pooler hostname for this project |
| PR #5: auto pooler via `SUPABASE_ACCESS_TOKEN` | **`HTTP 403: Forbidden`** | Management API pooler config endpoint rejects the personal access token |

**Latest CI env (run 26344741064):**

| Secret | Set? |
|--------|------|
| `SUPABASE_URL` | ✅ (fixed mid-session; was empty earlier) |
| `SUPABASE_DB_PASSWORD` | ✅ |
| `SUPABASE_ACCESS_TOKEN` | ✅ (present but **403 on API**) |
| `SUPABASE_DB_HOST` | ❌ empty |
| `SUPABASE_DB_PORT` | ❌ empty |

**Not in GitHub secrets:** `SUPABASE_DB_URL` (was tried earlier; wrong host caused tenant error).

---

## Connection resolution order (`scripts/apply_supabase_migrations.py`)

1. `SUPABASE_URL` + `SUPABASE_DB_PASSWORD` + **`SUPABASE_DB_HOST`** (+ optional `SUPABASE_DB_PORT`) → **use this in CI**
2. **CI only:** `SUPABASE_URL` + `SUPABASE_DB_PASSWORD` + `SUPABASE_ACCESS_TOKEN` → Management API lookup (**currently 403**)
3. `SUPABASE_DB_URL` or `SUPABASE_DB_POOLER_URL` (full URI)
4. Local fallback: direct `db.{ref}.supabase.co:5432`

Pooler username format: **`postgres.{project_ref}`** (not `postgres` alone).

---

## What works today

| Area | Status |
|------|--------|
| GitHub repo + CI scaffold | ✅ |
| `ci-validate` workflow | ✅ green on main |
| Local Supabase migrations | ✅ User ran `apply_supabase_migrations.py`; 001 bootstrapped, **002 applied** |
| `scripts/check_supabase_connection.py` | ✅ (assumed after local migrate) |
| Phase A docs | ✅ on main (via PR #2) |
| Constituents + yfinance OHLCV ingestion | ✅ merged PR #2 |
| DAX market seed | ✅ `de_dax` in registry + `storage/seeds/de_dax/constituents.csv` |
| Fundamentals Supabase schema | ✅ `002_fundamentals_mart.sql` (EBIT margin, no ROIC, no price cols) |

---

## Phase B (complete on main)

- `yf_fundamentals.parquet` ingestion, dbt through `mart_stock_cards`, `export_to_supabase.py`
- `check_pipeline_completeness.py` in Tier C (`data_pipeline.yml`)
- Streamlit app under `frontend/`
- Tier A/B CI in `ci-validate.yml`

**Still manual / product:** production pipeline smoke test, market activation audits, Streamlit Cloud deploy after weekly export.

---

## Authoritative product decisions (do not revert without user)

### Card metrics (5) — **EBIT margin, not ROIC**

User chose **option 2**: show **EBIT margin** via `ticker.info['operatingMargins']`. ROIC deferred (unreliable from yfinance).

| # | Metric | Source |
|---|--------|--------|
| 1 | Forward P/E | `forwardPE` |
| 2 | EBIT margin | `operatingMargins` × 100 |
| 3 | Revenue growth YoY | `revenueGrowth` × 100 |
| 4 | Net debt / EBITDA | `netDebt / ebitda` |
| 5 | FCF margin | FCF / Total Revenue (statements) |

- **No fallbacks** — missing any metric → not card-eligible
- Sector median with direction-aware wording (no traffic-light benchmarks)
- Save = watchlist; Skip = deprioritize (not permanent)
- No dating-app metaphors in docs

### Markets

**Active:** `us_sp500`, `uk_ftse100`, `jp_nikkei225`, `au_asx200`, `de_dax`  
**Inactive EU stubs:** `fr_cac40`, `nl_aex`, `ch_smi`, `es_ibex35` (audit before activate)

Registry: `docs/market_registry.yml` — single source of truth.

---

## Git / branch state (local machine may be stale)

**Remote `main`** includes (approximate order):

- PR #1 — engineering standards
- PR #2 — Phase A docs, fundamentals schema, DAX, automated migrations
- PR #3 — CI pooler URL
- PR #4 — `SUPABASE_DB_HOST` secret support
- PR #5 — auto pooler via `SUPABASE_ACCESS_TOKEN` (**merged**; migrate still fails on 403)

**Local checkout (end of session):** branch `fix/supabase-access-token-pooler`, tracking origin. **Local `main` was behind remote** — run `git fetch && git checkout main && git pull` before new work.

**Merged PRs for Supabase CI:** #3, #4, #5.

---

## GitHub Actions secrets (as of 2026-05-23)

Present in repo settings:

- `SUPABASE_URL`
- `SUPABASE_DB_PASSWORD`
- `SUPABASE_ACCESS_TOKEN`
- `SUPABASE_SERVICE_ROLE_KEY`

Missing (needed for Plan A fix):

- `SUPABASE_DB_HOST`
- `SUPABASE_DB_PORT`

---

## Local `.env` shape (values in user's machine only)

User has configured:

- `SUPABASE_URL`
- `SUPABASE_ANON_KEY` (new `sb_publishable_...` format, not JWT)
- `SUPABASE_SERVICE_ROLE_KEY` (new `sb_secret_...` format)
- `SUPABASE_DB_PASSWORD`
- `SUPABASE_ACCESS_TOKEN` (`sbp_...`) — **403 on pooler API locally**

Never commit `.env`. `.env.example` may be slightly out of date vs current CI approach.

---

## Key files for tomorrow

| File | Purpose |
|------|---------|
| `scripts/apply_supabase_migrations.py` | Migration runner; pooler URL resolution |
| `scripts/discover_supabase_db_host.py` | **Brute-force probe pooler hosts** → prints `SUPABASE_DB_HOST` |
| `scripts/print_supabase_pooler_config.py` | Test Management API token (currently 403) |
| `.github/workflows/supabase-migrate.yml` | Runs migrate on `main` + manual dispatch |
| `.github/workflows/data_pipeline.yml` | Stub export; needs secrets + real export script |
| `supabase/migrations/001_initial_schema.sql` | Legacy mart (price fields) |
| `supabase/migrations/002_fundamentals_mart.sql` | Fundamentals mart, DAX, drops price cols |
| `docs/supabase_setup.md` | **Stale** — still emphasizes `SUPABASE_DB_URL`; update after CI green |
| `docs/north_star.md`, `docs/data_contract.md` | Product + data contract |
| `docs/working_agreement.md` | Agent behaviour rules |

---

## Tomorrow's task list (suggested order)

### 1. Unblock CI (highest priority)

- [ ] Run `python scripts/discover_supabase_db_host.py` locally
- [ ] Add `SUPABASE_DB_HOST` + `SUPABASE_DB_PORT` to GitHub secrets
- [ ] Re-run `supabase-migrate` → expect **"all migrations already applied"** (002 already applied locally + remotely if manual)
- [ ] Update `docs/supabase_setup.md` to document **host/port secrets** as primary CI path; Management API as optional/fallback
- [ ] Optionally: improve 403 error message in script ("token lacks permission; use discover script or set SUPABASE_DB_HOST")

### 2. Optional: fix Management API path

- [ ] Verify token at https://supabase.com/dashboard/account/tokens (regenerate if needed)
- [ ] Test: `curl -H "Authorization: Bearer $TOKEN" https://api.supabase.com/v1/projects/nkfxrrazavlawggylbgd/config/database/pooler`
- [ ] If still 403, treat auto-lookup as best-effort only; document host/port approach

### 3. Phase B (after CI green)

- [ ] Fundamentals ingestion → `storage/raw/yf_fundamentals.parquet`
- [ ] dbt models per `docs/data_contract.md`
- [ ] `export_to_supabase.py`
- [ ] Wire `data_pipeline.yml`
- [ ] Streamlit MVP

---

## Working agreements (user preferences)

- **No manual SQL in Supabase Dashboard** — migrations via script/CI only
- **Feature branches + PRs** for repo changes
- **Do not commit** unless user explicitly asks
- **Do not revert ROIC** or other product decisions without approval
- User dislikes manual copy/paste workflows — prefer scripts (`discover_*`, `apply_*`)

---

## Useful commands

```bash
# Local: find pooler host for GitHub secrets
python scripts/discover_supabase_db_host.py

# Local: apply migrations (direct connection)
python scripts/apply_supabase_migrations.py

# Local: test Management API token (currently fails 403)
python scripts/print_supabase_pooler_config.py

# CI: re-run migrate
gh workflow run supabase-migrate.yml
gh run list --workflow=supabase-migrate.yml --limit 3
gh run view <run-id> --log-failed

# Sync local main
git fetch origin && git checkout main && git pull
```

---

## Prior conversation

Full transcript: agent transcript `ac3236be-edc8-4e71-abc0-c466a3363df4` (Cursor agent-transcripts folder).

---

*End of handover — delete or archive this file once CI is green and docs are updated.*
